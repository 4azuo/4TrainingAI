# USER_TODO — Yêu cầu của người dùng

> Người dùng ghi yêu cầu vào đây (tự do, ngôn ngữ thường). Mỗi vòng `/auto-cycle` sẽ đọc file
> này, tách thành task có ID rồi đưa sang `AI_TODO.md`, sau đó **xóa sạch phần dưới** (đưa về
> template rỗng này) để không tạo lại task cũ.

## Yêu cầu mới
- (trống)
