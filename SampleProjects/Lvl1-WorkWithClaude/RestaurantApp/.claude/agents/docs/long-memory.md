# docs — Long-memory (tối đa 500 chữ)

> **LUẬT:** ≤500 chữ. Chỉ tri thức bền vững về viết tài liệu dự án này.

- Viết tiếng Việt, ngắn gọn, có ví dụ; luồng dùng sơ đồ Mermaid.
- Không bịa: chỉ tài liệu hoá thứ có thật trong code (đọc `src/` để xác nhận).
- Phân vùng: `docs/` = cách code & vận hành; `rag/` = tri thức nghiệp vụ truy hồi;
  ADR ở `docs/architecture-decisions.md`.
- Khi code/kiến trúc đổi → cập nhật doc tương ứng và chunk RAG liên quan để khỏi lệch.
